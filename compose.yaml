version: '3'

services:
  app:
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
    image: app
    ports:
      - "80:80"
    networks:
      - app-network
    volumes:
      - ./:/var/www/html

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: simply_shop
      MYSQL_USER: root1
      MYSQL_PASSWORD: root1
    ports:
      - "3306:3306"
    networks:
      - app-network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
    ports:
      - "8081:80"
    networks:
      - app-network

  certbot:
    build: ./docker/cerebot

networks:
  app-network:
